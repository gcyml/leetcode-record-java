class Solution {
    public int findComplement(int num) {
        int n = 0;
        int tmp =num;
        while(num != 0){
            n = (n << 1)+1; 
            num = num >>1;
        }
        return ~tmp & n;
    }
}