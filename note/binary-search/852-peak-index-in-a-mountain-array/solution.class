class Solution {
    public int peakIndexInMountainArray(int[] A) {
        int left = 0, right = A.length -1;
        while(right > 1 && A[right] < A[right-1]){
            right--;
        }
        return right;
    }
}